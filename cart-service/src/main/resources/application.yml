server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://postgres-cart:5432/cartdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect